// //creating middleware as practice
// module.exports.practice = (req, res, next) => {
//   if (!models.Sessions.isLoggedIn(req.session)) {
//     res.redirect('/login')
//   } else {
//     next()
//   }
// };
